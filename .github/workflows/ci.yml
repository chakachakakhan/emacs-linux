name: CI

on:
  push:
  pull_request:

jobs:
  hello:
    runs-on: ubuntu-24.04

    steps:
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Use jq
        run: |
          echo '{"project":"emacs-linux","ci":true}' | jq
